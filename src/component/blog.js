import React, { Component } from "react";

export const Blog = () => {
  return (
    <div className="blog">
      {
        // iterate each element in the array
        // monsters (specific props)
        // key: what element to update if any of these elements changes
        // <Card image=" " caption=" " />
      }
    </div>
  );
};

export default Blog;
